# game_data/handlers/__init__.py
# Handler modules are loaded dynamically by the engine.
# Each file corresponds to a location_id (e.g., jazz_club.py → location "jazz_club").
# Only create a handler file if a location needs special logic.
